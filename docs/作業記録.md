# 作業記録

## 2026-07-01（初日）

### やったこと

1. **要件把握**
   - 素材は名刺画像1枚（`IMG_5847.jpeg`）のみ。→ `_reference/business-card.jpeg` へ退避（gitignore）。
   - 名刺から会社情報・事業内容・ロゴ色を読み取り。

2. **開発環境構築**
   - `create-next-app` で Next.js 16 + TypeScript + Tailwind CSS v4 を `Desktop\village` に構築。
   - `npm run build` 成功（型エラーなし・静的生成OK）。

3. **コーポレートサイト制作（ドラフト）**
   - トップ1枚構成。Header / Hero / Services / Strengths / Company / Contact / Footer を実装。
   - 会社情報は `app/lib/company.ts` に一元化。
   - SEOメタ・OGP・構造化データ（JSON-LD）・Noto Sans JP 対応。
   - `npm run dev` で表示確認（http://localhost:3000）。

4. **GitHub 公開**
   - https://github.com/daimag/village-hp.git の `main` へ push。

5. **ヒアリングシート作成**
   - sanadaroko の形式を参考に `openpyxl` で Excel 生成。
   - 当初8シート → 客先が身構えるとの指摘で **1シートに集約**。
   - 各項目のドラフト内容＋記入欄、写真リスト、デザイン/参考URL、**希望ドメイン欄**を収録。

### 決定事項・メモ

- 未確定情報（設立・資本金・従業員数・許可番号・営業時間・FAX 等）はヒアリングで回収予定。
- 確認用サイトはまだ未公開（localhostのみ）。次回 Vercel 等で公開URLを用意する。
- ロゴはSVGで暫定再現。正式データ入手後に差し替え。

### 次回やること

- 確認用サイト公開 → ヒアリングシート表紙のURL欄に反映
- ヒアリングシート送付（必要なら送付メール文面作成）
- 回収内容の反映 / 写真・ロゴ差し替え / 希望ドメイン取得

## 2026-07-21

- **パッケージマネージャを npm → pnpm に移行。** `package-lock.json` を削除、`pnpm install` で `pnpm-lock.yaml` 生成、`package.json` に `packageManager: pnpm@10.29.3` を追記。`sharp` のビルドを許可（`.npmrc` の onlyBuiltDependencies）。`pnpm build` 成功を確認。ドキュメント（README/CLAUDE/PROJECT/各手順書）も pnpm 表記へ統一。
- 開発環境の引き渡し関連資料を整備（`docs/打合せ資料_2026-07-22_開発環境引き渡し.md`、簡易マニュアル）。

## 2026-07-22

### 打合せ（議事録）
- **方針転換：法人向け(B2B)大型案件 → 個人向け(B2C)集客サイトへ。** 空き家・遺品整理〜解体のワンストップを主軸。来月中(8月)公開目標。議事録：`docs/20260722_打合せ/議事録_整理版.md`、写真素材：同フォルダ `ホームページ作成/`（解体前後の施工例・社長写真）。
- 社長は写真でなく「漫画風イラスト」で登場希望（`社長イラスト.jpg`）。

### サイト全面刷新（個人向け集客＋マルチページ化）
- **全ページ独立化＋ナビ日本語化**：`会社紹介(/about) / 事業内容(/service) / 施工例(/works) / 遺品整理(/ihin-seiri) / ブログ(/blog) / お知らせ(/news) / 会社概要(/company)`。ナビは各独立ページへ直結。サブページも共通の本ヘッダー（ナビ付き・戻る廃止）に統一。
- **施工例**：解体ビフォーアフター＋費用目安（確定2組の実写、`public/works/`）。TOP一部＋詳細ページ。
- **遺品整理・生前整理**：ネット相場反映、お悩み／対応サービス8／選ばれる理由／費用目安／流れ／対応エリア／FAQ／**写真欄（後日差し替え枠）**。
- **事業内容**：各サービスの詳細ページ（動的ルート `/service/[slug]` 5件）。
- **近隣対策**：大きな数字のグラフィカル6枚に刷新（養生・散水・時間帯配慮等を追加、他社事例参照）。
- **代表あいさつ**：AI作成文＋**漫画風イラスト**（`public/company/president.jpg`）を会社概要／会社案内に。
- 全体フォント補強（太さ・濃さ）、ABOUTほかで個人向け解体を強く訴求。

### ブログCMS（yumehoken方式＝Decap CMS＋コンテンツ専用リポ）
- `app/lib/posts.ts`（区分：記事/お知らせ）、一覧/詳細（/blog・/news）、`public/admin/`（Decap）、`app/api/auth`・`api/callback`（GitHub OAuth）、`scripts/fetch-content.mjs`（build時取り込み）。
- 本番設定：コンテンツリポ **daimag/village-hp-content**（Public）、Deploy Hook＋Actions（自動再デプロイ）、OAuthアプリ、Vercel環境変数。**/admin から投稿→自動公開まで稼働確認済み。** 手順：`docs/BLOG設置手順書.md`。投稿アカウント village-hubuser を content リポに招待。

### ロゴ
- 名刺（`_reference/business-card.jpeg`）のロゴを解析。写真直抜きは品質不足のため、**忠実なSVGで再現**（`app/components/LogoMark.tsx`：緑＋青シェブロン＋黒スウッシュ）→ ヘッダーの「V」ボックスと差し替え。

### 本番・インフラ
- **本番公開：`https://village2024.jp`（Vercel／daimag）。** コード repo は `daimag/village-hp`（村分離は一旦保留・daimag配下で運用）。ドメイン・メールはコアサーバー契約済み（`_reference/アカウント情報.md`）。
- 今日の変更は複数回 push 済み（サイト刷新・ブログ・ヘッダー統一）。**ロゴSVGはpush待ち（本人確認後に反映予定）。**

### 次回・保留
- ロゴSVGの本番反映（＋必要ならフッター/ヒーロー用の白版、"Village"ワードマーク付きフルロゴ）。
- 施工例の前後ペア・金額を社長に確認して確定。写真欄へ実写投入。
- 社長あいさつ文の社長チェック。MEO/SEO（門司 解体）・Google口コミ導線。

## 2026-07-23

### ヘッダー・ロゴ
- **サブページを本ヘッダー（ナビ付き）に統一**、「← トップへ戻る」廃止（`SubHeader`→`Header solid`）。
- **名刺ロゴをSVG再現**（`LogoMark`：緑＋青シェブロン＋黒スウッシュ）→ヘッダー採用。背景別に**色版/白版を自動切替**（ソリッド=色、ヒーロー透過/フッター=白）。
- **favicon をロゴに差し替え**（デフォルト削除）＋`apple-icon.png`。※favicon.icoは一度RGBでビルドエラー→**RGBAで再生成して復旧**。
- ヒーローの緑の斜め線 ＼ を削除。

### 表示・SEO/OGP
- **ヒーローを実写クロスフェード＋ズームインのスライドショー**に（現状写真→建った家→解体後の更地）。
- **OGP画像**（`public/ogp.png`）＋metadata（metadataBase/canonical/twitter）。**sitemap.ts / robots.ts**。
- 全体フォント補強、リード文・お悩みリストの文字を拡大（可読性）。

### 集客コンテンツ（議事録の方針＝個人向けB2C）
- トップに **お悩み／解体費用の目安（相場ベース）／FAQ（FAQPage構造化データ）／対応エリア** を追加、集客LP順に再構成。
- **遺品整理**：整理→搬出→清掃→**解体**のワンストップ工程（イラスト付き）、対応サービス8項目にアイコン。
- 受領写真を活用：施工例に**対応建物ギャラリー**、会社紹介に**社長＋ご子息の実写**（顔の見える経営）。

### お問い合わせ
- **独立ページ `/contact` 化**（TOPから撤去）、`mailto`廃止→**サーバー送信（`/api/contact`＋nodemailer＋コアサーバーSMTP）**。
- Vercel環境変数（SMTP_HOST=v2008.coreserver.jp / PORT=465 / USER=info@village2024.jp / PASS / MAIL_TO=info@village2024.jp）設定済み。**テスト送信→info@village2024.jp 着信確認済み**（返信先＝送信者）。

### 公開前対応
- **検索エンジン非表示**：全ページ `noindex,nofollow` ＋ robots.txt 全ブロック（`app/robots.ts`にコメントで公開時の戻し方あり）。正式公開時に解除。

### 先方向け成果物（ID/PW含むため非公開）
- 実装報告メール下書き：`_reference/実装報告メール_2026-07-23.md`（実装内容＋ブログ投稿方法＋ID/PW＋「検索非表示中」の旨）。
- **ブログ投稿マニュアル（画像入りDOCX）**：`docs/ブログ投稿マニュアル_ヴィレッジ.docx`（gitignore）。

### 現状
- 本番 `https://village2024.jp`（daimag/Vercel）。ブログCMS・お問い合わせフォーム稼働。**公開前＝検索非表示中**。
- CMS投稿アカウント：village-hubuser（`_reference/アカウント情報.md`）。

### 次回・保留
- 正式公開時に **noindex解除＋robots/ sitemap を公開用に戻す**。
- 送信元表示を info@village2024.jp に統一（コアサーバー既定送信ドメイン設定）／SPF・DKIM設定で到達性向上。
- 施工例の前後ペア・金額確定、写真欄へ実写投入。ブログSEO記事の投入。Google口コミ/MEO導線。

## 2026-07-31 〜 2026-08-04

### 受領資料の分析（工事前・後.xlsx）
- 社長より `docs/20260731_受領/工事前・後.xlsx` を受領（メール本文「工事前、後のイメージをこんな感じにしていただければ」＝**掲載依頼**）。
- 全数分析：**実質5件**（6枠目は写真0枚＋5件目と全項目一字一句同一＝テンプレの複製）。埋め込み写真10枚を抽出。
- 判明した既存データの誤り：**既存の施工例a（a-before/a-after）は今回の祇園と同一写真**（MD5一致）で、付随データ「木造2階建て／門司区／約150万円〜」が実際（鉄骨造・八幡東区祇園・185万円）と相違 → 是正。
- ギャラリー g1・g5 も c5-before・c1-after と同一写真 → 同一ページ内の重複掲載を回避。

| # | 所在地 | 坪数 | 構造 | 工期 | 工事金 |
|---|---|---|---|---|---|
| c1 | 八幡西区南八千代町 | 32坪 | 木造 | 10日 | 180万円 |
| c2 | 八幡東区帆柱 | 30坪 | コンクリートブロック、木造 | 20日 | 200万円 |
| c3 | 八幡東区祇園 | 30坪 | 鉄骨造 | 11日 | 185万円 |
| c4 | 門司区藤松 | 38坪 | 木造 | 11日 | 170万円 |
| c5 | 八幡西区若葉 | 25坪 | 木造 | 10日 | 135万円 |

### 施工例（/works・トップ抜粋）
- `cases` を2件→**5件**に差し替え。所在地・坪数・構造・**工事日数**・工事金を実績値で掲載（`DemolitionCase` に size / days を追加）。
- 写真10枚を `public/works/c1〜c5-{before,after}.jpg` として書き出し。
- カードを刷新（案A採用）：写真を4:3（施工例ページで約320px）、中央の矢印カラム廃止→境目に円形ピボット、坪数・構造・工期をタイル表示、工事金を最大40px、数字は tabular-nums。

### デザイン方針の転換（余白 → 端を破る）
- 「余白をたっぷり」ではダイナミズムが出ないため、**full-bleed（画面端まで抜く）**方針へ。
- `--shell:1460px` / `--gut:clamp(22px,4vw,64px)` / **`--bleed`**（本文の端から画面端までの距離）を変数化。`margin-right:calc(-1 * var(--bleed))` でどの要素も端まで抜ける。
- **`.split` を共通部品化**（`.split`＝写真右／`.split.alt`＝写真左）。ABOUT・トップ遺品整理・/about冒頭・/ihin-seiriイントロに適用（うち遺品整理2箇所は写真を撤去して現在はテキスト構成）。
- **角丸を全面廃止**（写真・タイル・箱すべて 0。ロゴマークのみ維持）。
- タイル共通：枠線依存をやめ上端に緑バー（4割幅→ホバーで全幅）、ホバーで6px浮上、**番号を86〜96pxに拡大してタイル上端で切る**。3つの強みは3枚を縦にずらす（margin-topで実装＝スクロール演出のtransformと競合させない）。
- CONCERNS：長文6件の箇条書き → **アイコン＋短い見出し＋補足のタイル3列**（`ConcernIcon` 6種を新規作成）。背景に受領写真（若葉の空き家）＋ぼかし・脱色フィルタ。
- SERVICE：全面の緑ベタ（不透明度.92）が背景写真を潰していたため階調に変更。主力の建物解体工事を2列ぶち抜きの大判カードに。
- 代表あいさつ：写真を4:5・背後に緑の面、署名を写真下端に重ねるプレート化、保有資格3点を追加、導入段落のみ拡大。
- **全体の文字サイズを引き上げ**（本文系は clamp で画面幅追従）。詳細はコミット `78d65d8`。

### スクロール表示アニメーション
- `ScrollReveal`（クライアント）が `main` 配下の `.wrap` 直下要素を IntersectionObserver で監視。各コンポーネントは無改修。カード群は**1枚ずつ**90msずつ遅らせて出す。
- ちらつき防止に `layout.tsx` のインラインスクリプトで描画前に `<html>.rv` を付与。JS無効なら付かず常時表示、1.5秒経っても起動しなければ保険で解除。例外時・対象0件時も `.rv` を外す。
- **不具合と修正**：非表示ルールに `:is()` を使い、詳細度が引数の最大値（`.service .grid > .card`＝クラス3個）に膨張して表示ルールに打ち勝ち、**本文が opacity:0 のまま出なくなった**（本番で発生）。`:where()`（詳細度0）へ変更して解消（`7888e1b`）。

### /ihin-seiri の整理
- 「こんなお悩みはありませんか？」「選ばれる理由」を削除（社長指示）。構成は イントロ→SERVICE→ONE-STOP→PRICE→FLOW→AREA→FAQ。
- 作業実績（写真）は**6枠すべて「写真準備中」の空枠**だったため、写真が1枚も無いあいだはセクションごと非表示に（1枚登録すれば自動復帰）。
- 未使用化した `estateWorries` / `estateReasons` と対応CSSを削除。

### 事故：無関係な画像の混入
- 遺品整理に `public/designs/img/interior.jpg` を**ファイル名だけで判断して採用**したが、中身は遺品整理と無関係な画像（海外の音楽アルバムのジャケット）。2箇所から撤去し、**ファイル自体も削除**。
- **教訓：画像は必ず中身を目視してから使う。** `/about` に採用した `002.jpg`（現場で図面を確認する2人）は確認済み。

### メール設定マニュアル
- `メール設定マニュアル_Android_iPhone` → **`メール設定マニュアル_iPhone_Android_PC`** に改訂（PC＝Thunderbird／Outlook を追加）。生成元 `docs/generate_mail_manual_docx.py`。
- 文面を業務文書のトーンへ（「設定は5〜10分で終わります」「入力するのは、この5つだけ」等の所要時間の断定・口語表現を削除。見出しを「① 設定情報」「⑤ 送受信テスト」「⑥ トラブル対応」に）。
- パスワードは本文に書かず別紙参照（そのまま渡せる構成）。**BAKは `docs/BAK/20260731/`（旧版）と `docs/BAK/20260731_v1/`（PC追加・言い回し修正前）の2世代**。

### コピー修正
- キャッチから「門司区」を外し「北九州」に統一（ヒーロー／会社紹介／サイトタイトル／OG／代表あいさつ）。**対応エリアの区名列挙・住所・構造化データ・各ページのdescriptionは据え置き**（ローカル検索用に「門司区」を残す）。

### 次回・保留
- **写真の原本**：掲載中の10枚は Excel 埋め込みの書き出しで **640×481** まで圧縮済み。原本があれば差し替え（full-bleed でさらに大きく見せるのも原本待ち）。
- **c3（祇園）の工事前写真に他社の看板＋電話番号**が写り込み。ぼかすか不掲載かの判断を要確認。
- 工事金の条件（税込/税別、残置物処分・ブロック塀・庭木等の附帯込みか）。特に c2（20日・6.7万/坪）は要確認。
- 施工例の町名まで掲載する場合の**施主の掲載許諾**。
- **`public/designs/img/` の中身未確認ファイル**（001・003〜005・1〜3・hero・demo1・demo2・excavator）の目視確認。`interior.jpg` の件があるため要点検。
- 木造の坪単価目安：`costGuide` は「約4〜5万円/坪」だが実績3件平均は **5.1万円/坪**（4.5〜5.6万）。要調整。
- /ihin-seiri の AREA・FAQ を残すかの判断。対応サービス8件も量が多い。
- 正式公開時に **noindex解除＋robots/sitemap を公開用に戻す**（`layout.tsx` の robots ／ `app/robots.ts`）。
